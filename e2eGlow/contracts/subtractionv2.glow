#lang glow

@interaction([A, B])
let subtraction = () => {
    let handA = 50 - 20;
    deposit! A -> handA;//30

    let handB = 30 - 10;
    deposit! B -> handB;//20

    withdraw! A <- handB; //90
    withdraw! B <- handA  // 210

};