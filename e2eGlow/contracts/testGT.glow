#lang glow

@interaction([A, B])
let fewif = (parA, parB) => {
    let handA = parA;
    deposit! A -> handA;

    let handB = parB;

    if(parA > 10){
       withdraw! A <- handA
    }else{
       withdraw! B <- handB
    }

};